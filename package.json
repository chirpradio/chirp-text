{
  "name": "chirp-text-poc",
  "version": "0.0.1",
  "description": "Static text-only mirror of chirpradio.org for screen readers / no-JS / crawlers",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-fetch": "^4.0.1"
  },
  "type": "module"
}
